name: 🧪 Test Workflow

on:
  push:
    branches: [ staging ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test step
      run: echo "Test workflow is working"
