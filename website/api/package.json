{
  "name": "contact-form-api",
  "version": "1.0.0",
  "description": "AWS Lambda function for processing contact form submissions",
  "main": "contact-form.js",
  "dependencies": {
    "@aws-sdk/client-ses": "^3.450.0"
  },
  "scripts": {
    "deploy": "zip -r contact-form.zip . && aws lambda update-function-code --function-name contact-form-api --zip-file fileb://contact-form.zip"
  }
}
